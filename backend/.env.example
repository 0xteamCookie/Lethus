# PostgreSQL — connection string for Prisma
DATABASE_URL="postgresql://lethus:lethus_password@localhost:5432/lethus_db?schema=public"

# Milvus — address of the Milvus gRPC port
MILVUS_ADDRESS=localhost:19530

# OpenAI — used for embeddings (text-embedding-3-small) and cold path LLM calls
OPENAI_API_KEY=sk-your-key-here

# Upstream LLM — where the actual user-facing requests get forwarded
UPSTREAM_LLM_URL=https://api.openai.com/v1/chat/completions
UPSTREAM_LLM_KEY=sk-your-key-here

# Server
PORT=3000

# Embedding model — text-embedding-3-small produces 1536-dimensional vectors
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536

# Milvus collection name
MILVUS_COLLECTION=turn_embeddings

# Algorithm tuning — safe defaults, adjust after testing
COLD_START_THRESHOLD=5
RETRIEVAL_TOKEN_BUDGET=2000
RECENT_TURNS_COUNT=3
STATE_DOC_UPDATE_INTERVAL=3
KADANE_THETA=1.0
GAIN_SHIFT=0.6
CHANGELOG_BOOST=1.0
CHANGELOG_NEIGHBOR_BOOST=0.3
